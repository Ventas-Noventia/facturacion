import test from "node:test";import assert from "node:assert/strict";import {ReportesService} from "../backend/services/reportesService.mjs";test("genera resumen",()=>{const r=new ReportesService().construir([{estatus:"TIMBRADA_MOCK",total:100,origen:"WHATSAPP"},{estatus:"BORRADOR",total:50,origen:"ECOMMERCE"}]);assert.equal(r.totalFacturas,2);assert.equal(r.timbradas,1);assert.equal(r.totalFacturado,100);assert.equal(r.porOrigen.WHATSAPP,1)});