export class ReportesService{construir(f=[]){const t=f.filter(x=>x.estatus==="TIMBRADA_MOCK"),b=f.filter(x=>x.estatus==="BORRADOR"),c=f.filter(x=>x.estatus==="CANCELADA");const po={};for(const x of f)po[x.origen||"OTRO"]=(po[x.origen||"OTRO"]||0)+1;return{totalFacturas:f.length,timbradas:t.length,borradores:b.length,canceladas:c.length,totalFacturado:t.reduce((s,x)=>s+Number(x.total||0),0),porOrigen:po}}}
